# 🚨 KIRO v6.0 MCP 서버 커넥션 에러 해결 가이드

## 📊 문제 상황 분석
- **에러 서버**: sequential-thinking, desktop-commander, arxiv-search
- **에러 유형**: 32000 포트 커넥션 에러
- **영향 범위**: 신규 추가된 3개 MCP 서버

## 🔍 원인 분석

### 1. 패키지 설치 문제
**가능성**: uvx로 설치된 패키지가 실제로 존재하지 않음
```bash
# 확인 명령어
uvx list
uvx --help
```

### 2. 포트 충돌 문제
**가능성**: 32000 포트가 이미 사용 중이거나 방화벽 차단
```bash
# 포트 사용 확인
netstat -an | findstr :32000
```

### 3. 패키지명 불일치
**가능성**: 실제 패키지명과 설정의 패키지명이 다름

## 🛠️ 해결 방안

### 방안 1: 패키지 존재 여부 확인 및 수정
실제 존재하는 패키지명으로 수정 필요

### 방안 2: 대체 구현 사용
존재하지 않는 패키지의 경우 대체 구현 또는 비활성화

### 방안 3: 포트 설정 변경
포트 충돌 해결을 위한 설정 변경