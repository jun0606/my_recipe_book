# 안전한 리팩토링 적용 계획

## 1. 파생 레시피 기능 리팩토링 적용 단계

### 단계 1: 서비스 및 유틸리티 클래스 적용
1. `RecipeDerivationService` 클래스 추가 (완료)
2. `RecipeTreeItem` 위젯 추가 (완료)
3. `RecipeActionsMenu` 위젯 추가 (완료)

### 단계 2: 테스트 및 검증
1. 각 서비스 및 위젯을 개별적으로 테스트
   - `RecipeDerivationService`의 각 메서드 테스트
   - `RecipeTreeItem` 위젯 렌더링 테스트
   - `RecipeActionsMenu` 위젯 동작 테스트

### 단계 3: 점진적 적용
1. `RecipeTreeScreen` 리팩토링 적용
   - 기존 화면과 리팩토링된 화면을 비교 테스트
   - 모든 기능이 동일하게 작동하는지 확인
   - 문제가 없으면 리팩토링된 버전으로 교체

2. `RecipeDetailScreen`에 `RecipeActionsMenu` 적용
   - 기존 메뉴와 리팩토링된 메뉴를 비교 테스트
   - 모든 메뉴 항목이 동일하게 작동하는지 확인
   - 문제가 없으면 리팩토링된 버전으로 교체

3. `AddRecipeScreen`에 `RecipeDerivationService` 적용
   - 복사 및 파생 레시피 생성 기능 테스트
   - 기존 기능과 동일하게 작동하는지 확인
   - 문제가 없으면 리팩토링된 버전으로 교체

### 단계 4: Provider 등록 및 통합
1. `main.dart`에 새로운 Provider 등록
   - `CookingModeProvider` 등록
   - `RecipeCalculationProvider` 등록
   - `RecipeDerivationService` 등록
   - 앱 시작 및 기본 기능 테스트

## 2. 기능 검증 체크리스트

### 레시피 복사 기능
- [ ] 레시피 복사 메뉴 항목 표시
- [ ] 복사 시 모든 데이터 정확히 복제
- [ ] 이미지 파일 복사 기능
- [ ] 복사본 제목에 "(복사본)" 추가
- [ ] 복사본은 parentId 없이 독립적인 레시피로 저장

### 파생 레시피 기능
- [ ] 파생 레시피 생성 메뉴 항목 표시
- [ ] 파생 시 모든 데이터 정확히 복제
- [ ] 이미지 파일 복사 기능
- [ ] 파생본 제목에 "(파생)" 추가
- [ ] 파생본은 원본 레시피의 ID를 parentId로 저장

### 파생 관계 추적 기능
- [ ] 레시피 목록에서 파생 레시피 개수 표시
- [ ] 파생된 레시피에 "(파생됨)" 표시
- [ ] 레시피 트리 화면에서 계보 정확히 표시
- [ ] 부모-자식 관계 시각적으로 명확히 표현

## 3. 롤백 계획

### 문제 발생 시 롤백 절차
1. 각 단계별로 변경 사항 백업
2. 문제 발생 시 해당 파일만 원래 버전으로 복원
3. 전체 기능에 문제가 있을 경우 모든 변경 사항 롤백

### 데이터 안전성 확보
1. 데이터베이스 스키마 변경 없음 확인
2. 기존 파생 관계 데이터 유지 확인
3. 사용자 데이터 손실 방지 확인

## 4. 최종 검증

### 종합 테스트
1. 모든 리팩토링된 기능 테스트
2. 엣지 케이스 및 오류 상황 테스트
3. 사용자 시나리오 기반 종합 테스트

### 성능 검증
1. 리팩토링 전후 성능 비교
2. 메모리 사용량 및 응답성 확인
3. 대용량 데이터 처리 시 성능 확인