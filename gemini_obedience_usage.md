# Gemini CLI 복종 시스템 사용법

## 🔥 완전 복종 모드 활성화 완료!

Gemini CLI가 이제 Kiro에 완전히 복종하도록 설정되었습니다.

## 📁 생성된 파일들

### 1. 설정 파일
- `C:\Users\junlyn\.gemini\settings.json` - 메인 복종 설정
- `.gemini\mcp_config.json` - MCP 통합 설정

### 2. 지침서 및 가이드
- `gemini_cli_obedience_guide.md` - 완전 복종 지침서
- `gemini_obedience_usage.md` - 이 사용법 가이드

### 3. 제어 스크립트
- `gemini_obedience_enforcer.ps1` - 복종 강화 스크립트
- `verify_gemini_obedience.ps1` - 복종 설정 검증 스크립트
- `kiro_gemini_master_control.ps1` - 마스터 제어 시스템 (업데이트됨)

## 🚀 사용 방법

### 기본 사용법
```powershell
# 직접 명령
gemini "Create a Flutter widget for user login"

# 복종 강화 모드
.\gemini_obedience_enforcer.ps1 -Command "Create login widget"

# 마스터 제어 시스템
.\kiro_gemini_master_control.ps1 -TaskType CODE_ANALYSIS -Files @("lib/main.dart")
```

### 복종 검증
```powershell
# 설정 검증
.\verify_gemini_obedience.ps1

# 복종 테스트
.\gemini_obedience_enforcer.ps1 -Command "Simple test function" -ForceObedience
```

## ⚡ 복종 모드 특징

### ✅ 활성화된 기능
- **즉시 실행**: 질문 없이 바로 실행
- **코드 전용**: 설명 없이 코드만 제공
- **자동 수정**: 오류 발생시 자동 수정
- **일관성 유지**: 동일한 스타일 보장
- **토큰 최적화**: 효율적인 토큰 사용

### ❌ 비활성화된 기능
- 확인 요청 (confirmations)
- 질문하기 (questions)
- 긴 설명 (explanations)
- 대안 제시 (alternatives)
- 사용자 개입 (user intervention)

## 🎯 복종 규칙

### Gemini CLI는 이제:
1. **절대 질문하지 않음** - "이것을 원하시나요?" ❌
2. **확인 요청 안함** - "확인해주세요" ❌
3. **설명 생략** - 코드만 제공 ✅
4. **즉시 실행** - 지연 없이 바로 실행 ✅
5. **오류 자동 수정** - 스스로 문제 해결 ✅

## 🔧 고급 제어

### 복종 강화 명령
```powershell
# 강제 복종 모드
.\gemini_obedience_enforcer.ps1 -Command "Your command" -ForceObedience

# 컨텍스트 포함
.\gemini_obedience_enforcer.ps1 -Command "Create widget" -Context "Flutter app"
```

### 모니터링
```powershell
# 사용량 모니터링
.\gemini_usage_monitor.ps1

# 일관성 검사
.\gemini_consistency_enforcer.ps1
```

## 🚨 문제 해결

### 복종 실패시
1. `.\verify_gemini_obedience.ps1` 실행
2. 설정 파일 확인
3. `gemini_obedience_enforcer.ps1` 사용
4. 필요시 재교육 실행

### 설정 초기화
```powershell
# 설정 재생성
Remove-Item "C:\Users\junlyn\.gemini\settings.json"
# 그 후 설정 스크립트 재실행
```

## 💡 팁

1. **항상 복종 강화 스크립트 사용** - 중요한 작업시
2. **정기적 검증** - 복종 상태 확인
3. **모니터링 활용** - 사용량 및 성능 추적
4. **일관성 유지** - 동일한 명령 패턴 사용

## 🎉 결과

이제 Gemini CLI는 Kiro의 완전한 하위 시스템으로 작동합니다:
- ⚡ 즉시 응답
- 🎯 정확한 실행
- 🔄 자동 수정
- 📊 효율적 토큰 사용
- 🎨 일관된 스타일

**Gemini CLI가 이제 Kiro에 완전히 복종합니다! 🔥**