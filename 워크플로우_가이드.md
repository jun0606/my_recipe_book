#  워크플로우 가이드 - 필수 제공 파일 목록

##  작업 시작시 필수 제공 파일

### 1.  메인 가이드 (최우선)
```
KIRO_MASTER_GUIDE.md
```
- 전체 시스템 개요
- MCP 서버 사용법
- 워크플로우 패턴
- 문제 해결 가이드

### 2.  현재 상태 파악
```
docs/next_steps_action_plan.md
```
- 현재 진행 상황
- 완료된 작업 목록
- 다음 단계 계획
- 성과 지표

### 3.  MCP 설정 및 사용법
```
.kiro/settings/mcp.json
mcp_usage_guide.md
```
- 활성화된 MCP 서버 목록
- 각 서버별 사용법
- 자동 승인 설정

### 4.  메모리 및 컨텍스트
```
SQLite 데이터베이스 조회:
- session_context 테이블
- mcp_performance_monitor 테이블

Memory MCP 엔티티 검색:
- 최근 작업 관련 엔티티
```

##  워크플로우 벗어날 때 제공 파일

### 1.  백업 폴더 전체
```
워크플로우_백업_2025_07_29/
├── README.md
├── mcp_shared_access.md
├── mcp_설정.json
├── 워크플로우_진행상황.md
└── Memory_MCP_워크플로우_엔티티.md
```

### 2.  프로젝트 스펙 파일
```
.kiro/specs/practical-recipe-calculator/tasks_v2_final.md
.kiro/specs/baking-calculator-improvement/tasks.md
.kiro/specs/baking-calculator-improvement/design.md
```

### 3.  현재 상태 스냅샷
```
SQLite 쿼리 결과:
SELECT * FROM session_context ORDER BY created_at DESC LIMIT 10;

Memory MCP 엔티티:
최근 워크플로우 관련 모든 엔티티
```

##  세션 연속성을 위한 필수 정보

### 1. 현재 작업 컨텍스트
- 마지막 완료된 Task 번호
- 진행 중인 작업 상세 내용
- 사용 중인 MCP 서버 목록

### 2. 프로젝트 상태
- Git 상태 (변경된 파일)
- 테스트 통과 현황
- 빌드 상태

### 3. 설정 및 환경
- MCP 서버 활성화 상태
- 자동화 규칙 설정
- 성능 최적화 설정

##  빠른 체크리스트

###  작업 시작시
- [ ] KIRO_MASTER_GUIDE.md 제공
- [ ] docs/next_steps_action_plan.md 제공
- [ ] SQLite session_context 최근 10개 기록 조회
- [ ] Memory MCP 워크플로우 엔티티 검색
- [ ] MCP 서버 상태 확인

###  워크플로우 벗어날 때
- [ ] 워크플로우_백업_2025_07_29/ 폴더 전체 제공
- [ ] 현재 진행 상황 SQLite에 기록
- [ ] Memory MCP에 현재 상태 저장
- [ ] 다음 세션을 위한 컨텍스트 정리

##  핵심 명령어

### 현재 상태 확인
```sql
-- SQLite에서 최근 작업 확인
SELECT * FROM session_context ORDER BY created_at DESC LIMIT 5;
```

### Memory MCP 검색
```
mcp_memory_search_nodes -query "워크플로우"
mcp_memory_search_nodes -query "Task"
```

### 백업 폴더 확인
```
워크플로우_백업_2025_07_29/ 폴더 내 모든 파일
```

## 💡 중요 원칙

1. **항상 백업 먼저**: 작업 시작 전 현재 상태 백업
2. **컨텍스트 보존**: 세션 간 연속성 보장
3. **MCP 활용**: 모든 작업에 MCP 서버 우선 사용
4. **진행 상황 기록**: SQLite와 Memory MCP에 실시간 기록

이 가이드를 따르면 언제든지 워크플로우를 재개하거나 새로운 작업을 시작할 수 있습니다! 🚀